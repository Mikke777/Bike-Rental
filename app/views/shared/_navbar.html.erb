<nav class="navbar">
  <div class="navbar-left">
    <i class="fa-solid fa-bicycle" style="color: #FFA500;"></i>
    <span class="brand-name">Rent-A-Bike</span>
  </div>

  <div class="navbar-right">
    <!-- This icon will be shown on smaller screens -->
    <i class="fa-solid fa-bars" id="menuIcon"></i>

    <!-- The collapsible menu -->
    <div class="menu-links">
      <%= link_to "Home", root_path %>
      <%= link_to "Create offer", new_bike_path %>
      <%= link_to "Dashboard", dashboard_path %>

      <% if user_signed_in? %>
        <p class="signed-in-user"><%= current_user.email %></p>
        <i class="fa-solid fa-user" style="color: #FFA500;"></i>
        <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class: "login-signup" %>
      <% else %>
        <i class="fa-solid fa-user" style="color: #FFA500;"></i>
        <%= link_to "Sign in", new_user_session_path, data: {turbo_method: :get}, class: "login-signup" %>
      <% end %>
    </div>
  </div>
</nav>
